import { getTypes } from './babelInstance'

export transformExistentialExpression(path) ->
  t = getTypes()

  path.replaceWith(
    t.binaryExpression(
      "!=",
      path.node.argument,
      t.nullLiteral()
    )
  )
